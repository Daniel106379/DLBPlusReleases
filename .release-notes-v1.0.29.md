## DLBPlus 1.0.29

### What's new
- **Backend-first Home artwork guard:** skips post-paint TMDb enrichment for DLBPlus backend rails when backend home catalog loads (fixes landscape card flicker for BACKEND and legacy FALLBACK modes)
- **Catalog source cleanup:** stored FALLBACK override migrates to BACKEND; settings no longer displays FALLBACK as Backend silently
- **Continue Watching hero:** metadata sync on enrichment (from 1.0.28)

### Includes from 1.0.28
- CW hero metadata/synopsis refresh without focus change
- Initial backend-only artwork skip guard
- Simplified Catalog Source settings UI

### Install / update
- In-place update from **v1.0.0** through **v1.0.28** is supported (same `dlbplus.jks` signing key).
- Primary asset: `app-universal-release.apk`
